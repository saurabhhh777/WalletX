# Backend Environment Variables
# Copy this file to .env for local development
# For production, set these in your deployment platform (Railway, Heroku, etc.)

# Server Configuration
PORT=5000
NODE_ENV=development
# NODE_ENV=production (for production deployment)

# MongoDB Connection
# Local Development
MONGODB_URI=mongodb://localhost:27017/walletx
# Production (MongoDB Atlas)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/walletx?retryWrites=true&w=majority

# JWT Secret (CHANGE THESE IN PRODUCTION!)
# Generate a strong random string for production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Session Secret (CHANGE THESE IN PRODUCTION!)
# Generate a strong random string for production
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# OAuth Configuration (Optional - for Google/GitHub login)
# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Backend URL (for OAuth callbacks)
# Development
BACKEND_URL=http://localhost:5000
# Production (Update with your deployed backend URL)
# BACKEND_URL=https://your-backend-domain.com

# Frontend URL (for CORS)
# Development
FRONTEND_URL=http://localhost:5173
# Production (Update with your deployed frontend URL)
# FRONTEND_URL=https://walletx-git-master-asksaurabh.vercel.app

# Optional: Logging and Monitoring
# LOG_LEVEL=info
# SENTRY_DSN=your-sentry-dsn

# Optional: Rate Limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Optional: Security
# CORS_ORIGIN=https://your-frontend-domain.com
# TRUST_PROXY=true

# Note: Never commit .env files to version control
# These are example values - replace with your actual configuration 